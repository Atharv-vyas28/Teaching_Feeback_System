<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Application routes (AI-owned)
|--------------------------------------------------------------------------
| This file belongs to the generated application: LaraCopilot re-emits it
| as a COMPLETE file to add the app's OWN routes — custom (non-CRUD)
| controller endpoints and public-facing pages (search, browse, detail,
| booking/order flow, etc).
|
| It ships EMPTY. The pre-baked admin auth, dashboard, and user-management
| routes are STATIC in routes/web.php — they are NOT here and must NOT be
| re-declared (doing so duplicates/breaks them). Blueprint's resource routes
| are appended to web.php automatically. Add ONLY the app's own routes here.
*/